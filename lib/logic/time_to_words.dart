abstract class TimeToWords {
  /// Converts a [DateTime] object into a human-readable string representation of time.
  /// Implementations MUST include English comments next to all foreign translations
  /// to explain the meaning of the words and the logic flow.
  String convert(DateTime time);
}
